<Window x:Class="FinalLabSystem.Views.Settings.StockAdjustmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تعديل المخزون" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="المادة:" FontWeight="Bold" FontSize="14" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" x:Name="MaterialNameText" FontSize="14" Foreground="#333" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="المخزون الحالي:" FontWeight="Bold" FontSize="14" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="3" x:Name="CurrentStockText" FontSize="14" Foreground="#333" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="4" Text="التعديل (+ للازدياد، - للنقص):" FontWeight="Bold" FontSize="14" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox x:Name="AdjustmentBox" Width="100" Height="30" FontSize="14"
                     HorizontalContentAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock VerticalAlignment="Center" Text="السبب (اختياري):" Margin="8,0,0,0"/>
            <TextBox x:Name="ReasonBox" Width="150" Height="30" FontSize="14" Margin="4,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button x:Name="SaveButton" Content="حفظ" Width="80" Height="32" Margin="0,0,8,0"
                    Click="SaveButton_Click" Background="#1976D2" Foreground="White" FontWeight="Bold"/>
            <Button x:Name="CancelButton" Content="إلغاء" Width="80" Height="32"
                    Click="CancelButton_Click" Background="#757575" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
