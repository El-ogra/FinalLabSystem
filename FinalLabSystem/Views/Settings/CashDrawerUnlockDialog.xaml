<Window x:Class="FinalLabSystem.Views.Settings.CashDrawerUnlockDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="فتح درج النقدية"
        Height="220" Width="380"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="أدخل كلمة مرور درج النقدية"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,12"
                   HorizontalAlignment="Center"/>

        <PasswordBox Grid.Row="1" x:Name="PasswordBox"
                     Height="30" Margin="0,0,0,8"
                     KeyDown="PasswordBox_KeyDown"/>

        <TextBlock Grid.Row="2" x:Name="ErrorText"
                   Foreground="Red" FontSize="12"
                   Visibility="Collapsed" Margin="0,0,0,8"
                   HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="3" Text="كلمة المرور:"
                   FontSize="12" Foreground="#666" Margin="0,0,0,4"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal"
                    HorizontalAlignment="Center" Margin="0,12,0,0">
            <Button Content="فتح" Click="OK_Click"
                    Width="80" Height="30" Margin="8,0"
                    Background="#1976D2" Foreground="White" FontWeight="Bold"/>
            <Button Content="إلغاء" Click="Cancel_Click"
                    Width="80" Height="30" Margin="8,0"
                    Background="#E53935" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
