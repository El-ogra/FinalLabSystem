<Window x:Class="FinalLabSystem.Views.Patients.BarcodeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="طباعة الباركود" Height="420" Width="620"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft">
    <DockPanel Margin="12">
        <DataGrid ItemsSource="{Binding Tubes}" SelectedItem="{Binding SelectedTube}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="الباركود" Binding="{Binding BarcodeValue}" Width="*"/>
                <DataGridTextColumn Header="نوع الأنبوب" Binding="{Binding TubeType}" Width="140"/>
                <DataGridTextColumn Header="لون الأنبوب" Binding="{Binding TubeColor}" Width="120"/>
                <DataGridTemplateColumn Header="طباعة" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="طباعة" Command="{Binding DataContext.PrintBarcodeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button DockPanel.Dock="Bottom" Content="طباعة الكل" Command="{Binding PrintAllCommand}" Height="36" Margin="0,10,0,0"/>
    </DockPanel>
</Window>
