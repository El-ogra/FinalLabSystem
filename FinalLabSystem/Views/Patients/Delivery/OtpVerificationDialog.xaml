<Window x:Class="FinalLabSystem.Views.Patients.Delivery.OtpVerificationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تأكيد التسليم بـ OTP" Height="300" Width="350"
        WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="أدخل رمز التحقق المكوّن من 6 أرقام" FontSize="14" Margin="0,0,0,16"/>

        <TextBox Grid.Row="1" Text="{Binding EnteredOtp, UpdateSourceTrigger=PropertyChanged}"
                 MaxLength="6" FontSize="20" HorizontalContentAlignment="Center"
                 Height="36" Margin="0,0,0,12">
            <TextBox.InputScope>
                <InputScopeName NameValue="Digits"/>
            </TextBox.InputScope>
        </TextBox>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="Red" Margin="0,0,0,8"
                   TextWrapping="Wrap"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
            <Button Content="تحقق" Command="{Binding VerifyCommand}" Width="80" Height="32" Margin="0,0,8,0"
                    IsDefault="True"/>
            <Button Content="إعادة إرسال" Command="{Binding ResendOtpCommand}" Width="90" Height="32" Margin="0,0,8,0"/>
            <Button Content="إلغاء" Command="{Binding CancelCommand}" Width="80" Height="32"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
