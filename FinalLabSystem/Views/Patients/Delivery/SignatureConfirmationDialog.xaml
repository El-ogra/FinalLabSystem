<Window x:Class="FinalLabSystem.Views.Patients.Delivery.SignatureConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تأكيد التسليم بالتوقيع" Height="500" Width="450"
        WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="توقيع المستلم" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="اسم المستلم:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Text="{Binding ReceivedByName, UpdateSourceTrigger=PropertyChanged}" Width="250" Height="28"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Background="White" Margin="0,0,0,12">
            <InkCanvas x:Name="SignatureInkCanvas" Background="White">
                <InkCanvas.DefaultDrawingAttributes>
                    <DrawingAttributes Color="Black" Width="2" Height="2" FitToCurve="True"/>
                </InkCanvas.DefaultDrawingAttributes>
            </InkCanvas>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="Red" Margin="0,0,0,8">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding StatusMessage}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="مسح" Command="{Binding ClearCommand}" Width="80" Height="32" Margin="0,0,8,0"/>
            <Button Content="تأكيد" Command="{Binding ConfirmCommand}" Width="80" Height="32" Margin="0,0,8,0"
                    IsDefault="True"/>
            <Button Content="إلغاء" Command="{Binding CancelCommand}" Width="80" Height="32"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
