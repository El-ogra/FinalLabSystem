<Window x:Class="FinalLabSystem.Views.Patients.DeliveryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تسليم نتائج التحاليل" Height="400" Width="520"
        WindowStartupLocation="CenterScreen" FlowDirection="RightToLeft">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="تسليم نتائج التحاليل" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="اسم المريض:" FontWeight="SemiBold" Margin="0,0,8,0"/>
            <TextBlock Text="{Binding PatientName}" FontSize="14"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="Green" FontSize="14"
                   FontWeight="SemiBold" Margin="0,0,0,16">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding StatusMessage}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <StackPanel Grid.Row="3" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="تأكيد بالتوقيع" Command="{Binding ConfirmWithSignatureCommand}"
                    Width="180" Height="45" FontSize="14" Margin="0,0,0,12"/>
            <Button Content="تأكيد بـ OTP" Command="{Binding ConfirmWithOtpCommand}"
                    Width="180" Height="45" FontSize="14"/>
        </StackPanel>

        <Button Grid.Row="4" Content="العودة" Command="{Binding ReturnToMainCommand}"
                Width="120" Height="36" HorizontalAlignment="Left"/>
    </Grid>
</Window>
