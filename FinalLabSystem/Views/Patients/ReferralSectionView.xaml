<UserControl x:Class="FinalLabSystem.Views.Patients.ReferralSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="White"
            BorderBrush="#CCCCCC"
            BorderThickness="1"
            CornerRadius="4"
            Padding="8"
            Margin="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="90"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto" MinWidth="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="اللقب" VerticalAlignment="Center" Margin="0,3"/>
            <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding ReferralTitles}" Text="{Binding ReferralTitle, UpdateSourceTrigger=PropertyChanged}" IsEditable="True" Width="80" HorizontalAlignment="Right" Margin="0,3" Height="26"/>
            <Label Grid.Row="0" Grid.Column="2" Content="جهة التحويل" VerticalAlignment="Center" Margin="8,3,0,3"/>
            <ComboBox Grid.Row="0" Grid.Column="3" ItemsSource="{Binding ReferralSuggestions}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedReferral}" DisplayMemberPath="SourceName" IsEditable="True" Margin="0,3" Height="26"/>

            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,3" VerticalAlignment="Center">
                <CheckBox Content="حفظ جهة التحويل" IsChecked="{Binding ShouldSaveReferral}" VerticalAlignment="Center"/>
                <CheckBox Content="طباعة جهة التحويل" IsChecked="{Binding PrintReferralOnReport}" Margin="12,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <Label Grid.Row="1" Grid.Column="2" Content="العنوان" VerticalAlignment="Center" Margin="8,3,0,3"/>
            <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding ReferralAddress}" Margin="0,3" Height="26"/>
        </Grid>
    </Border>
</UserControl>
