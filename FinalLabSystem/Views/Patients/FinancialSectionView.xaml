<UserControl x:Class="FinalLabSystem.Views.Patients.FinancialSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border BorderBrush="#2196F3"
            BorderThickness="1,0,0,0"
            Background="#F8F9FA"
            Padding="8,4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <UniformGrid Grid.Column="0" Columns="2" Rows="4" Margin="0,0,8,0">
                <Label Content="الإجمالي" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Subtotal, Mode=OneWay}" IsReadOnly="True" Background="#FFFDE7" Margin="4" Height="26"/>
                <Label Content="خصم قيمة" VerticalAlignment="Center"/>
                <TextBox Text="{Binding DiscountAmount, UpdateSourceTrigger=PropertyChanged}" Margin="4" Height="26"/>
                <Label Content="خصم %" VerticalAlignment="Center"/>
                <TextBox Text="{Binding DiscountPercent, UpdateSourceTrigger=PropertyChanged}" Margin="4" Height="26"/>
                <Label Content="بعد الخصم" VerticalAlignment="Center"/>
                <TextBox Text="{Binding TotalAfterDiscount, Mode=OneWay}" IsReadOnly="True" Background="#FFFDE7" Margin="4" Height="26"/>
            </UniformGrid>

            <UniformGrid Grid.Column="1" Columns="2" Rows="4" Margin="8,0">
                <Label Content="المدفوع" VerticalAlignment="Center"/>
                <TextBox Text="{Binding AmountPaid, UpdateSourceTrigger=PropertyChanged}" Margin="4" Height="26"/>
                <Label Content="الباقي للمريض" VerticalAlignment="Center"/>
                <TextBox Text="{Binding BalanceDue, Mode=OneWay}" IsReadOnly="True" Background="#FFEBEE" Margin="4" Height="26"/>
                <Label Content="الباقي للمعمل" VerticalAlignment="Center"/>
                <TextBox Text="{Binding BalanceDue, Mode=OneWay}" IsReadOnly="True" Background="#FFEBEE" Margin="4" Height="26"/>
                <Label Content="المدفوع سابقاً" VerticalAlignment="Center"/>
                <TextBox Text="{Binding PreviouslyPaid, Mode=OneWay}" IsReadOnly="True" Background="#F5F5F5" Margin="4" Height="26"/>
            </UniformGrid>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="8,0,0,4">
                <Button Content="موافقة" Command="{Binding ConfirmPaymentCommand}" Background="#4CAF50" Foreground="White" Padding="12,4" MinWidth="80"/>
                <Button Content="تراجع" Command="{Binding RevertCommand}" Padding="12,4" Margin="8,0,0,0" MinWidth="80"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
